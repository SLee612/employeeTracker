const http = require('mysql');

const hostname = mysql.createConnection ({
  host: "localhost",

 port = 3000,

 user: "root",
 
 password: "root",
 database: "employee_trackerDB",
});

RTCPeerConnection.connect(function(err) {
  if (err) throw err;

});

module.exports = connection;



